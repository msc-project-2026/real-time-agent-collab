{
  "id": "webex-meeting-login",
  "name": "Webex Meeting Login",
  "description": "Logs the dedicated meeting agent into Webex through the sandboxed Brave browser at gateway startup.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      }
    }
  }
}
