{
  "name": "@openclaw/deploy-agent",
  "version": "1.0.0",
  "description": "Deployment agent for OpenClaw: drives a deploy-cli on the hosting VPS to deploy apps from git repos",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "@collab/github": "*"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
