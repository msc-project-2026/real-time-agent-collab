{
  "id": "webex",
  "name": "Webex Channel",
  "description": "Cisco Webex messaging integration for OpenClaw",
  "channels": ["webex"],
  "contracts": {
    "tools": ["collab_append_pending_message", "collab_claim_pending_batch"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": false
      },
      "token": {
        "type": "string",
        "description": "Webex bot access token"
      },
      "webhookUrl": {
        "type": "string",
        "description": "Public URL for receiving Webex webhook POSTs"
      },
      "webhookSecret": {
        "type": "string",
        "description": "HMAC-SHA1 secret for verifying webhook signatures"
      },
      "dmPolicy": {
        "type": "string",
        "enum": ["allow", "deny", "allowlisted"],
        "default": "deny",
        "description": "Policy for direct messages"
      },
      "allowFrom": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Allowed sender emails or person IDs (dmPolicy: allowlisted)"
      },
      "accessToken": {
        "type": "string",
        "description": "OAuth access token for receiving all space messages"
      },
      "refreshToken": {
        "type": "string",
        "description": "OAuth refresh token for renewing the access token"
      },
      "clientId": {
        "type": "string",
        "description": "Webex Integration client ID"
      },
      "clientSecret": {
        "type": "string",
        "description": "Webex Integration client secret"
      },
      "defaultMeetingNotesRoomId": {
        "type": "string",
        "description": "Fallback Webex roomId to post meeting notes into when a transcript's meeting has no resolvable roomId"
      }
    }
  }
}
