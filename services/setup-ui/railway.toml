[build]
builder = "RAILPACK"
buildCommand = "cd services/setup-ui && npm install && npm run build"

[deploy]
startCommand = "node services/setup-ui/server.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3